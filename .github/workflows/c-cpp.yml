name: C/C++ CI

on:
  push:
    branches: [ "master", "develop" ]
  pull_request:
    branches: [ "master", "develop" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    # - name: Build Project
    #   run: make

    - name: Run Tests
      run: make tests

    # - name: Distribution Check
    #   run: make distcheck
